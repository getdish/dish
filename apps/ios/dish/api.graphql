query SearchRestaurants($radius: Float!, $geo: geometry!) {
    restaurant(
        where: {
            location: {
                _st_d_within: {
                    distance: $radius,
                    from: $geo
                }
            }
        },
        limit: 30
    ) {
        name
        location
    }
}

