{
  "name": "dish-monorepo",
  "private": true,
  "workspaces": {
    "nohoist": [
      "**"
    ],
    "packages": [
      "packages/models",
      "packages/*",
      "apps/*",
      "services/*"
    ]
  },
  "scripts": {
    "start": "",
    "build": "tsc -b",
    "build:watch": "tsc -b -w --preserveWatchOutput",
    "test": "ava",
    "clean": "bin/clean.sh"
  },
  "devDependencies": {
    "ava": "^2.4.0",
    "prettier": "^1.19.1",
    "ts-node": "^8.5.4",
    "tsconfig-paths": "^3.9.0",
    "typescript": "^3.8.2",
    "wsrun": "^5.2.0"
  }
}
