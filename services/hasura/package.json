{
  "name": "@dish/hasura",
  "version": "0.0.1",
  "description": "",
  "devDependencies": {
    "hasura-cli": "^1.2.0"
  },
  "dependencies": {
    "@dish/auth": "*"
  },
  "scripts": {
    "build": "true",
    "test": "DISH_ENV=test ava test/main.ts --verbose",
    "watch": "true",
    "start": "docker-compose up"
  }
}
