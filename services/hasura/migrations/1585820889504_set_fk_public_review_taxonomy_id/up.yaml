- args:
    cascade: false
    read_only: false
    sql: |-
      alter table "public"."review"
                 add constraint "review_taxonomy_id_fkey"
                 foreign key ("taxonomy_id")
                 references "public"."taxonomy"
                 ("id") on update cascade on delete no action;
  type: run_sql
