- args:
    cascade: false
    read_only: false
    sql: |-
      CREATE TRIGGER region_triggers BEFORE INSERT OR UPDATE ON hrr
          FOR EACH ROW EXECUTE PROCEDURE region_triggers();

      CREATE TRIGGER nhood_triggers BEFORE INSERT OR UPDATE ON zcta5
          FOR EACH ROW EXECUTE PROCEDURE nhood_triggers();
  type: run_sql
