- args:
    cascade: false
    read_only: false
    sql: CREATE EXTENSION IF NOT EXISTS pgcrypto;
  type: run_sql
- args:
    cascade: false
    read_only: false
    sql: CREATE TABLE "public"."list_tag"("id" uuid NOT NULL DEFAULT gen_random_uuid(),
      "created_at" timestamptz NOT NULL DEFAULT now(), "list_id" uuid NOT NULL, "tag_id"
      uuid NOT NULL, PRIMARY KEY ("id") , UNIQUE ("list_id", "tag_id", "id"));
  type: run_sql
- args:
    name: list_tag
    schema: public
  type: add_existing_table_or_view
