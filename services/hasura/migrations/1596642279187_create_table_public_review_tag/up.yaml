- args:
    cascade: false
    read_only: false
    sql: CREATE EXTENSION IF NOT EXISTS pgcrypto;
  type: run_sql
- args:
    cascade: false
    read_only: false
    sql: CREATE TABLE "public"."review_tag"("id" uuid NOT NULL DEFAULT gen_random_uuid(),
      "tag_id" uuid NOT NULL, "review_id" uuid NOT NULL, "sentence" text NOT NULL,
      "sentiment" numeric NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("tag_id") REFERENCES
      "public"."tag"("id") ON UPDATE cascade ON DELETE cascade, FOREIGN KEY ("review_id")
      REFERENCES "public"."review"("id") ON UPDATE cascade ON DELETE cascade, UNIQUE
      ("tag_id", "review_id", "sentence"));
  type: run_sql
- args:
    name: review_tag
    schema: public
  type: add_existing_table_or_view
