- args:
    cascade: false
    read_only: false
    sql: |-
      alter table "public"."tag_tag" drop constraint "tag_tag_category_tag_id_fkey",
                   add constraint "tag_tag_category_tag_id_fkey"
                   foreign key ("category_tag_id")
                   references "public"."tag"
                   ("id") on update cascade on delete cascade;
  type: run_sql
