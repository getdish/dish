- args:
    cascade: false
    read_only: false
    sql: |-
      alter table "public"."review" drop constraint "review_tag_id_fkey",
                add constraint "review_taxonomy_id_fkey"
                foreign key ("tag_id")
                references "public"."tag"
                ("id")
                on update cascade
                on delete no action;
  type: run_sql
