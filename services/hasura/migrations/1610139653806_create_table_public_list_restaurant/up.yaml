- args:
    cascade: false
    read_only: false
    sql: CREATE EXTENSION IF NOT EXISTS pgcrypto;
  type: run_sql
- args:
    cascade: false
    read_only: false
    sql: CREATE TABLE "public"."list_restaurant"("id" uuid NOT NULL DEFAULT gen_random_uuid(),
      "restaurant_id" uuid NOT NULL, "list_id" uuid NOT NULL, "comment" text, PRIMARY
      KEY ("restaurant_id","list_id") , FOREIGN KEY ("restaurant_id") REFERENCES "public"."restaurant"("id")
      ON UPDATE cascade ON DELETE cascade, FOREIGN KEY ("list_id") REFERENCES "public"."list"("id")
      ON UPDATE cascade ON DELETE cascade, UNIQUE ("id"));
  type: run_sql
- args:
    name: list_restaurant
    schema: public
  type: add_existing_table_or_view
