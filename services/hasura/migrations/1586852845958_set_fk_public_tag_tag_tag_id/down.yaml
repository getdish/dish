- args:
    cascade: false
    read_only: false
    sql: |-
      alter table "public"."tag_tag" drop constraint "tag_tag_tag_id_fkey",
                add constraint "tag_tag_tag_id_fkey"
                foreign key ("tag_id")
                references "public"."tag"
                ("id")
                on update restrict
                on delete restrict;
  type: run_sql
