- args:
    cascade: false
    read_only: false
    sql: |-
      alter table "public"."tag"
                 add constraint "tag_parentId_fkey"
                 foreign key ("parentId")
                 references "public"."tag"
                 ("id") on update restrict on delete restrict;
  type: run_sql
