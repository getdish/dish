- args:
    name: regions
    table:
      name: list
      schema: public
    using:
      manual_configuration:
        column_mapping:
          id: list_id
        remote_table:
          name: list_region
          schema: public
  type: create_array_relationship
