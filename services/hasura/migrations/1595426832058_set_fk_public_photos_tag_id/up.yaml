- args:
    cascade: false
    read_only: false
    sql: |-
      alter table "public"."photos"
                 add constraint "photos_tag_id_fkey"
                 foreign key ("tag_id")
                 references "public"."tag"
                 ("id") on update cascade on delete cascade;
  type: run_sql
