{
  "name": "@dish/use-store",
  "version": "0.0.1",
  "main": "_/index.js",
  "tsmain": "src/index",
  "sideEffects": false,
  "private": true,
  "scripts": {
    "build": "tsc -b || true",
    "watch": "tsc -b --watch --preserveWatchOutput",
    "test": "ava test/* --verbose"
  },
  "dependencies": {
    "zustand": "^3.0.1",
    "use-atom": "^0.5.6"
  },
  "devDependencies": {
    "@dish/react-test-env": "*"
  },
  "peerDependencies": {
    "react": "*"
  }
}
