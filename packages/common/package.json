{
  "name": "@dish/common",
  "version": "0.0.1",
  "main": "_/index.js",
  "tsmain": "src/index",
  "sideEffects": false,
  "private": true,
  "scripts": {
    "prebuild": "rm -f tsconfig.tsbuildinfo",
    "build": "tsc -b",
    "watch": "tsc -b --watch --preserveWatchOutput",
    "test": "true"
  },
  "dependencies": {
    "@sentry/node": "^5.16.1"
  },
  "devDependencies": {
    "@dish/graph": "*",
    "@types/node": "^13.7.0",
    "dotenv": "^8.2.0",
    "find-up": "^4.1.0"
  }
}
