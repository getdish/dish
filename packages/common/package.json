{
  "name": "@dish/common",
  "version": "0.0.1",
  "main": "_/index.js",
  "tsmain": "src/index",
  "sideEffects": false,
  "private": true,
  "scripts": {
    "prebuild": "rm -f tsconfig.tsbuildinfo",
    "build": "tsc -b",
    "watch": "tsc -b --watch --preserveWatchOutput",
    "test": "true"
  },
  "dependencies": {
    "@dish/graph": "*",
    "@sentry/node": "^5.16.1",
    "find-up": "^4.1.0"
  },
  "devDependencies": {
    "@types/node": "^13.7.0",
    "dotenv": "^8.2.0"
  }
}
