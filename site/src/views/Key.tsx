import { Button, gloss } from '@o/ui'

export const Key = gloss(Button, {
  display: 'inline',
  size: 'xs',
  glint: false,
  fontWeight: 600,
  hoverStyle: false,
  activeStyle: false,
  showInnerElement: 'never',
})
